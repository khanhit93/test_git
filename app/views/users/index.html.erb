<%= provide(:title, t("titles.index")) %>
<h1>All User</h1>
<div class="row">
	<div class="col-md-8">
		<table class="table table-default">
			<% @users.each do |user| %>
				<%= render user %>
			<% end %>
			<%= will_paginate %>
		</table>
	</div>
</div>
